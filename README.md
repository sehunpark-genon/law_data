# 대한민국 법령 데이터 (트라이얼)

법제처 OpenAPI 수집 결과를 Markdown + payload(JSON)로 관리한다.

```
법령/{법}/{법률|시행령|시행규칙}.md   ← 현행(시행일≤오늘 최대). git log = 시행 이력
법령/{법}/payload/{종}.{시행일}.json   ← 시행일별 버전 전부(과거·현행·미래)
```

- 현행 = payload 중 시행일 ≤ 오늘 최대. 미래 시행분도 payload로 보관.
- 각 커밋 날짜 = 그 버전 시행일(공포일·번호는 커밋 메시지/frontmatter).
